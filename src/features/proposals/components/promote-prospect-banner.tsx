"use client";

// TODO(Task 19): Replace this stub with the real PROSPECT promotion banner.
// This placeholder exists so Task 17 can land in a compilable state.

export function PromoteProspectBanner(_props: {
	clientId: string;
	clientName: string;
}) {
	return (
		<div className="rounded-md border border-dashed bg-muted/40 p-3 text-muted-foreground text-sm">
			Promoção de prospect em breve.
		</div>
	);
}
