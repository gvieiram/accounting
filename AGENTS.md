# AGENTS.md

This repository uses `CLAUDE.md` as the single canonical project instruction file.

Codex and any other agent that reads `AGENTS.md` must immediately read and follow `CLAUDE.md` in this same directory. Do not duplicate project rules here; update `CLAUDE.md` instead.
